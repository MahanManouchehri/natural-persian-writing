# راهنمای کاربردی نگارش طبیعی فارسی

این مرجع را هنگام تولید یا بازنویسی متن فارسی در مستندات فنی، رابط کاربری، پیام‌های پشتیبانی، متن رسمی یا محتوای حقوقی بخوانید.

## اصل محوری

معنا، دقت فنی، قصد نویسنده، اصطلاحات و سطح رسمی‌بودن متن را حفظ کنید؛ ساختار نامأنوس جملهٔ انگلیسی را حفظ نکنید.

## مستندات فنی و API

لحن باید حرفه‌ای، فنی، خنثی، مستقیم و قابل مرور سریع باشد. رفتار قابل مشاهدهٔ endpoint را توضیح دهید و جزئیات پیاده‌سازی را فقط زمانی بیاورید که برای مصرف‌کنندهٔ API مهم است.

نامناسب:

`Notificationهای private کاربر جاری را صفحه‌بندی‌شده می‌خواند.`

مناسب:

`Notificationهای خصوصی کاربر فعلی را به‌صورت صفحه‌بندی‌شده برمی‌گرداند.`

نامناسب:

`Departmentهای active قابل انتخاب را برمی‌گرداند.`

مناسب:

`لیست Departmentهای فعال و قابل انتخاب را برمی‌گرداند.`

نامناسب:

`فقط Notification متعلق به actor را read می‌کند.`

مناسب برای عملیات Mark-as-Read:

`Notification موردنظر را برای کاربر فعلی به حالت خوانده‌شده درمی‌آورد.`

ورودی، خروجی و خطا را مستقیم توضیح دهید:

- `شناسه Department الزامی است.`
- `Notification به‌روزرسانی‌شده را برمی‌گرداند.`
- `اگر Department پیدا نشود، پاسخ 404 برمی‌گردد.`
- `اگر داده‌های ورودی معتبر نباشند، پاسخ 422 برمی‌گردد.`

از عبارت‌های مبهمی مانند «پاسخ مناسب بازگردانده خواهد شد» استفاده نکنید.

## مجوز و امنیت

قاعدهٔ دسترسی و نتیجهٔ نقض آن را به‌صورت یک جملهٔ طبیعی و دقیق توضیح دهید.

نامناسب:

`مالک Notification؛ IDOR برابر 403 است.`

مناسب:

`فقط مالک Notification می‌تواند این عملیات را انجام دهد. تلاش برای دسترسی به Notification کاربر دیگر با پاسخ 403 Forbidden رد می‌شود.`

IDOR یک دستهٔ آسیب‌پذیری است و با status code «برابر» نیست. بررسی مالکیت، مجوزها، نقش‌ها، محدودهٔ دید منابع و محدودیت تغییر وضعیت را در ساده‌سازی حذف نکنید.

## فارسی رسمی و حرفه‌ای

رسمی‌بودن را با عبارت‌های اداری و فعل‌های سنگین اشتباه نگیرید.

- به‌جای `کاربر می‌بایست نسبت به ارسال شناسه اقدام نماید.` بنویسید: `کاربر باید شناسه را ارسال کند.`
- به‌جای `در صورت عدم وجود Conversation مورد نظر پاسخ خطای 404 بازگردانده خواهد شد.` بنویسید: `اگر Conversation موردنظر پیدا نشود، پاسخ 404 برمی‌گردد.`
- به‌جای `این عملیات منجر به آزادسازی مکالمه از پشتیبان فعلی خواهد گردید.` بنویسید: `این عملیات گفت‌وگو را از پشتیبان فعلی آزاد می‌کند.`

در متن رسمی نیز فعل مستقیم و جملهٔ کوتاه‌تر معمولاً دقیق‌تر است.

## رابط کاربری و پیام خطا

متن رابط کاربری باید کوتاه باشد و قدم بعدی را روشن کند. جزئیات داخلی Backend را فقط زمانی نمایش دهید که برای کاربر مفید است.

- به‌جای `عملیات با عدم موفقیت مواجه گردید.` بنویسید: `عملیات انجام نشد.`
- اگر علت معلوم است، دقیق‌تر بنویسید: `فایل آپلود نشد. دوباره امتحان کنید.`
- به‌جای `مقدار واردشده نامعتبر می‌باشد.` بنویسید: `مقدار واردشده معتبر نیست.`
- اگر فیلد معلوم است، بنویسید: `شماره موبایل معتبر نیست.`

## پیام پشتیبانی

محترمانه، روشن و صمیمی باشید، بدون لحن اداری.

نامناسب:

`خواهشمند است نسبت به ارسال تصویر خطای مربوطه اقدام فرمایید.`

مناسب:

`لطفاً یک تصویر از خطایی که می‌بینید ارسال کنید.`

## فارسی محاوره‌ای

در گفت‌وگوی واقعاً غیررسمی می‌توان از صورت‌هایی مانند «می‌تونی»، «می‌خوای»، «اگه» و «چیکار» استفاده کرد. این املا را خودکار وارد مستندات فنی، تجاری، رسمی یا حقوقی نکنید.

## متن حقوقی، قراردادی و سیاستی

دقت بر ساده‌سازی اولویت دارد. تعهد، ممنوعیت، استثنا، دامنه، مسئولیت، اصطلاح تعریف‌شده و تضمین را آزادانه بازنویسی نکنید. فقط وقتی جمله را ساده کنید که اثر حقوقی آن تغییر نکند.

## کنترل کیفیت نهایی

از خود بپرسید:

1. آیا یک فارسی‌زبان حرفه‌ای این جمله را در همین موقعیت طبیعی می‌نویسد؟
2. آیا متن در اولین خواندن روشن است؟
3. آیا اصطلاحات پروژه و شناسه‌های کد دست‌نخورده مانده‌اند؟
4. آیا ساده‌سازی، قاعدهٔ فنی یا امنیتی را ضعیف کرده است؟
5. آیا واژهٔ انگلیسی باقی‌مانده واقعاً مفید یا بخشی از قرارداد پروژه است؟
6. آیا متن بیش از حد محاوره‌ای یا بی‌دلیل اداری شده است؟
